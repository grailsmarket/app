/** Touch long-press duration (ms) that opens a reaction popover/picker. */
export const LONG_PRESS_MS = 500

/** Movement (px) past which a touch is treated as a scroll and cancels the long-press. */
export const LONG_PRESS_MOVE_CANCEL_PX = 10
